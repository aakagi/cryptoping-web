import page from 'page';

export default app => {
  // Routes
  // page('/', () => store.set({ View: require('./views/index.svelte') }));
  // page('/fart', () => store.set({ View: require('./views/fart.svelte') }));
  page();
}
